name: CI

on:
  push:
    branches: [ "main" ]
    tags: [ "v*" ]
  pull_request:
    branches: [ "main" ]

jobs:
  ci:
    uses: ./.github/workflows/reusable-ci.yml
    with:
      force_all: false
    secrets: inherit
