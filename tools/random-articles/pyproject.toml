[project]
name = "random-articles"
version = "0.1.0"
description = "Fetch random analyzed articles from enabled feeds"
requires-python = ">=3.11"
dependencies = [
  "psycopg[binary]>=3.2.0",
]

[tool.uv]
package = false
