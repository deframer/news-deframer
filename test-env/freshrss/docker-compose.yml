---

services:
  freshrss:
    image: nginx:alpine
    ports:
      - "8001:80"
    networks:
      - test-env-network

networks:
  test-env-network:
    external: true
    name: deframer-test-env-net
