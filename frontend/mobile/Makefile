.PHONY: install start android ios web clean

install:
	npm install

start:
	npx expo start

android:
	npx expo start --android

ios:
	npx expo start --ios

web:
	npx expo start --web

clean:
	rm -rf node_modules .expo
